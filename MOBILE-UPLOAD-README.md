# FlyYab Bale Bot v1.0.2 RC3 — Mobile GitHub Package

This package is trimmed for mobile GitHub upload and contains only runtime/deploy files.
Tests and long documentation were excluded from this ZIP; the full RC3 already passed the complete local test gate.

Important:
- Keep BALE_AUTOMATION_ENABLED=0 during Live Gate.
- Existing Cloudflare secrets/variables stay in the dashboard.
- After upload, let Cloudflare build/deploy from GitHub.
